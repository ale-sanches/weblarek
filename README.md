# Проектная работа "Веб-ларек"

Стек: HTML, SCSS, TS, Vite

Структура проекта:
- src/ — исходные файлы проекта
- src/components/ — папка с JS компонентами
- src/components/base/ — папка с базовым кодом

Важные файлы:
- index.html — HTML-файл главной страницы
- src/types/index.ts — файл с типами
- src/main.ts — точка входа приложения
- src/scss/styles.scss — корневой файл стилей
- src/utils/constants.ts — файл с константами
- src/utils/utils.ts — файл с утилитами

## Установка и запуск
Для установки и запуска проекта необходимо выполнить команды

```
npm install
npm run dev
```

или

```
yarn
yarn dev
```
## Сборка

```
npm run build
```

или

```
yarn build
```
# Интернет-магазин «Web-Larёk»
«Web-Larёk» — это интернет-магазин с товарами для веб-разработчиков, где пользователи могут просматривать товары, добавлять их в корзину и оформлять заказы. Сайт предоставляет удобный интерфейс с модальными окнами для просмотра деталей товаров, управления корзиной и выбора способа оплаты, обеспечивая полный цикл покупки с отправкой заказов на сервер.

## Архитектура приложения

Код приложения разделен на слои согласно парадигме MVP (Model-View-Presenter), которая обеспечивает четкое разделение ответственности между классами слоев Model и View. Каждый слой несет свой смысл и ответственность:

Model - слой данных, отвечает за хранение и изменение данных.  
View - слой представления, отвечает за отображение данных на странице.  
Presenter - презентер содержит основную логику приложения и  отвечает за связь представления и данных.

Взаимодействие между классами обеспечивается использованием событийно-ориентированного подхода. Модели и Представления генерируют события при изменении данных или взаимодействии пользователя с приложением, а Презентер обрабатывает эти события используя методы как Моделей, так и Представлений.

### Базовый код

#### Класс Component
Является базовым классом для всех компонентов интерфейса.
Класс является дженериком и принимает в переменной `T` тип данных, которые могут быть переданы в метод `render` для отображения.

Конструктор:  
`constructor(container: HTMLElement)` - принимает ссылку на DOM элемент за отображение, которого он отвечает.

Поля класса:  
`container: HTMLElement` - поле для хранения корневого DOM элемента компонента.

Методы класса:  
`render(data?: Partial<T>): HTMLElement` - Главный метод класса. Он принимает данные, которые необходимо отобразить в интерфейсе, записывает эти данные в поля класса и возвращает ссылку на DOM-элемент. Предполагается, что в классах, которые будут наследоваться от `Component` будут реализованы сеттеры для полей с данными, которые будут вызываться в момент вызова `render` и записывать данные в необходимые DOM элементы.  
`setImage(element: HTMLImageElement, src: string, alt?: string): void` - утилитарный метод для модификации DOM-элементов `<img>`


#### Класс Api
Содержит в себе базовую логику отправки запросов.

Конструктор:  
`constructor(baseUrl: string, options: RequestInit = {})` - В конструктор передается базовый адрес сервера и опциональный объект с заголовками запросов.

Поля класса:  
`baseUrl: string` - базовый адрес сервера  
`options: RequestInit` - объект с заголовками, которые будут использованы для запросов.

Методы:  
`get(uri: string): Promise<object>` - выполняет GET запрос на переданный в параметрах ендпоинт и возвращает промис с объектом, которым ответил сервер  
`post(uri: string, data: object, method: ApiPostMethods = 'POST'): Promise<object>` - принимает объект с данными, которые будут переданы в JSON в теле запроса, и отправляет эти данные на ендпоинт переданный как параметр при вызове метода. По умолчанию выполняется `POST` запрос, но метод запроса может быть переопределен заданием третьего параметра при вызове.  
`handleResponse(response: Response): Promise<object>` - защищенный метод проверяющий ответ сервера на корректность и возвращающий объект с данными полученный от сервера или отклоненный промис, в случае некорректных данных.

#### Класс EventEmitter
Брокер событий реализует паттерн "Наблюдатель", позволяющий отправлять события и подписываться на события, происходящие в системе. Класс используется для связи слоя данных и представления.

Конструктор класса не принимает параметров.

Поля класса:  
`_events: Map<string | RegExp, Set<Function>>)` -  хранит коллекцию подписок на события. Ключи коллекции - названия событий или регулярное выражение, значения - коллекция функций обработчиков, которые будут вызваны при срабатывании события.

Методы класса:  
`on<T extends object>(event: EventName, callback: (data: T) => void): void` - подписка на событие, принимает название события и функцию обработчик.  
`emit<T extends object>(event: string, data?: T): void` - инициализация события. При вызове события в метод передается название события и объект с данными, который будет использован как аргумент для вызова обработчика.  
`trigger<T extends object>(event: string, context?: Partial<T>): (data: T) => void` - возвращает функцию, при вызове которой инициализируется требуемое в параметрах событие с передачей в него данных из второго параметра.

# Данные

В данном разделе описаны интерфейсы данных приложения, а также типы, используемые в них.

## Интерфейсы

### Тип TPayment

Описывает допустимые способы оплаты заказа.

```typescript
type TPayment = 'card' | 'cash';
```

### Интерфейс IProduct

Описывает структуру данных товара, представленного в каталоге магазина.

```typescript
interface IProduct {
  id: string;
  description: string;
  image: string;
  title: string;
  category: string;
  price: number | null;
}
```

**Поля интерфейса:**
- `id: string` — уникальный идентификатор товара.
- `description: string` — подробное текстовое описание товара.
- `image: string` — URL изображения товара.
- `title: string` — название товара.
- `category: string` — категория, к которой относится товар.
- `price: number | null` — цена товара в синапсах. Значение `null` означает, что товар недоступен для покупки.

### Интерфейс IBuyer

Описывает структуру данных покупателя, которые необходимы для оформления заказа.

```typescript
interface IBuyer {
  payment: TPayment;
  email: string;
  phone: string;
  address: string;
}
```

**Поля интерфейса:**
- `payment: TPayment` — выбранный способ оплаты: «онлайн» (`'card'`) или «при получении» (`'cash'`).
- `email: string` — адрес электронной почты покупателя.
- `phone: string` — номер телефона покупателя.
- `address: string` — адрес доставки заказа.

---

# Модели данных

В данном разделе описаны классы, реализующие хранение и работу с данными приложения. Классы разделены по смыслу и зонам ответственности в соответствии с архитектурой MVP.

## Класс ProductCatalog

**Назначение и зона ответственности:** отвечает за хранение полного списка товаров магазина, а также за хранение одного товара, выбранного пользователем для подробного просмотра. Является центральным хранилищем данных о доступных в магазине позициях.

**Конструктор:** не принимает параметров. При создании экземпляра массив товаров инициализируется как пустой, а поле для выбранного товара — как `null`.

**Поля класса:**
- `_items: IProduct[]` — массив объектов `IProduct`. Хранит полный список товаров, загруженных с сервера.
- `_preview: IProduct | null` — объект `IProduct` или `null`. Хранит товар, выбранный пользователем для просмотра в подробном модальном окне.

**Методы класса:**
- `setItems(items: IProduct[]): void` — принимает массив `IProduct[]` и сохраняет его в поле `_items`, заменяя предыдущее содержимое.
- `getItems(): IProduct[]` — возвращает `IProduct[]` — текущий массив всех товаров.
- `getItemById(id: string): IProduct | undefined` — принимает `id: string`. Возвращает `IProduct` с указанным идентификатором или `undefined`, если товар не найден.
- `setPreview(item: IProduct): void` — принимает объект `IProduct` и сохраняет его в поле `_preview` для последующего отображения.
- `getPreview(): IProduct | null` — возвращает `IProduct | null` — текущий товар для подробного отображения или `null`, если ни один товар не выбран.

## Класс Cart

**Назначение и зона ответственности:** отвечает за хранение и управление набором товаров, которые пользователь добавил в корзину. Содержит логику добавления, удаления, очистки товаров, а также расчёта итоговой стоимости и количества позиций.

**Конструктор:** не принимает параметров. При создании экземпляра массив товаров в корзине инициализируется как пустой.

**Поля класса:**
- `_items: IProduct[]` — массив объектов `IProduct`. Хранит товары, добавленные пользователем в корзину.

**Методы класса:**
- `getItems(): IProduct[]` — возвращает `IProduct[]` — текущий массив товаров в корзине.
- `addItem(item: IProduct): void` — принимает `IProduct`. Добавляет товар в массив `_items`, если его там ещё нет.
- `removeItem(item: IProduct): void` — принимает `IProduct`. Удаляет товар из массива `_items` по его `id`.
- `clear(): void` — очищает массив `_items`, удаляя все товары из корзины.
- `getTotalPrice(): number` — возвращает `number` — сумму цен всех товаров в корзине. Товары с ценой `null` не учитываются.
- `getCount(): number` — возвращает `number` — количество товаров в корзине.
- `hasItem(id: string): boolean` — принимает `id: string`. Возвращает `boolean` — `true`, если товар с указанным `id` присутствует в корзине, иначе `false`.

## Класс BuyerData

**Назначение и зона ответственности:** отвечает за хранение, изменение, валидацию и очистку персональных данных покупателя, которые тот вводит при оформлении заказа. Реализует логику проверки корректности заполнения полей формы.

**Конструктор:** не принимает параметров. При создании экземпляра все строковые поля инициализируются пустыми строками, способ оплаты — значением `'card'` по умолчанию.

**Поля класса:**
- `_payment: TPayment` — хранит выбранный покупателем способ оплаты заказа.
- `_address: string` — хранит адрес доставки, введённый покупателем.
- `_phone: string` — хранит номер телефона покупателя.
- `_email: string` — хранит адрес электронной почты покупателя.

**Методы класса:**
- `setField(field: keyof IBuyer, value: string): void` — принимает имя поля `field: keyof IBuyer` и значение `value: string`. Обновляет указанное поле, не затрагивая остальные. Позволяет сохранить, например, только адрес без изменения прочих данных.
- `getData(): IBuyer` — возвращает объект `IBuyer` — полный набор данных покупателя (способ оплаты, адрес, телефон, email).
- `clear(): void` — сбрасывает все поля покупателя к начальным (пустым) значениям.
- `validate(): Partial<Record<keyof IBuyer, string>>` — возвращает объект, содержащий только те поля, которые не прошли проверку. Ключ — имя поля, значение — текст сообщения об ошибке. Если все поля заполнены корректно (непусты), возвращается пустой объект. Пример возвращаемого значения при наличии ошибок:

```typescript
{
  payment: 'Не выбран вид оплаты';
  email: 'Укажите email';
}
```

Такой объект означает, что поля с адресом и телефоном прошли валидацию, а поля со способом оплаты и email содержат ошибки. Пустой объект сигнализирует о том, что форма заполнена корректно и может быть отправлена.

---

# Слой коммуникации

В данном разделе описан класс, отвечающий за взаимодействие приложения с сервером «Web-Larёk». Класс реализует коммуникационный слой через композицию: он принимает объект, реализующий интерфейс `IApi`, и использует его методы `get` и `post` для выполнения запросов.

## Типы данных для работы с сервером

Описаны в `src/types/index.ts`.

`TProductListResponse` — тип ответа сервера на запрос списка товаров. Переиспользует уже имеющийся дженерик `ApiListResponse<IProduct>`:

```typescript
type TProductListResponse = ApiListResponse<IProduct>;
// { total: number; items: IProduct[] }
```

`IOrderRequest` — тип тела запроса при оформлении заказа. Расширяет `IBuyer`, добавляя массив идентификаторов товаров и итоговую сумму:

```typescript
interface IOrderRequest extends IBuyer {
  items: string[];
  total: number;
}
```

`IOrderResponse` — тип ответа сервера при успешном оформлении заказа:

```typescript
interface IOrderResponse {
  id: string;
  total: number;
}
```

## Класс WebLarekApi

**Назначение и зона ответственности:** отвечает за все сетевые запросы приложения к API сервера «Web-Larёk». Инкапсулирует конкретные эндпоинты и форматы данных, предоставляя остальным частям приложения удобный интерфейс без прямой зависимости от базового класса `Api`.

**Конструктор:**  
`constructor(api: IApi)` — принимает объект `api`, реализующий интерфейс `IApi`. Сохраняет его в приватное поле для последующего использования в методах.

**Поля класса:**
- `_api: IApi` — объект, реализующий интерфейс `IApi`. Используется для выполнения HTTP-запросов.

**Методы класса:**
- `getProducts(): Promise<TProductListResponse>` — выполняет GET-запрос на эндпоинт `/product/`. Возвращает промис с объектом `TProductListResponse`, содержащим общее количество товаров (`total`) и массив товаров (`items: IProduct[]`).
- `createOrder(order: IOrderRequest): Promise<IOrderResponse>` — принимает объект `order: IOrderRequest` с данными покупателя, списком id товаров и итоговой суммой. Выполняет POST-запрос на эндпоинт `/order/`. Возвращает промис с объектом `IOrderResponse`, содержащим идентификатор заказа (`id`) и подтверждённую сумму (`total`).
---

# Слой представления (View)

Классы слоя View отвечают за отображение данных на странице. Каждый класс управляет своим блоком разметки. Все классы наследуются от базового класса `Component`.

В ответ на любое действие пользователя генерируется событие через `EventEmitter`, которое обрабатывается Презентером.

## Класс Page

**Назначение:** управляет корневыми элементами страницы — галереей карточек и счётчиком корзины в шапке.

**Конструктор:**  
`constructor(container: HTMLElement, events: IEvents)` — принимает корневой элемент страницы и брокер событий.

**Поля класса:**
- `_catalog: HTMLElement` — контейнер галереи карточек (`.gallery`)
- `_counter: HTMLElement` — элемент счётчика корзины (`.header__basket-counter`)
- `_basketButton: HTMLButtonElement` — кнопка открытия корзины (`.header__basket`)

**Методы класса:**
- `set catalog(items: HTMLElement[]): void` — принимает массив DOM-элементов карточек и отображает их в галерее.
- `set counter(value: number): void` — принимает `number`, обновляет счётчик товаров в корзине.

---

## Класс Card

**Назначение:** базовый класс для всех карточек товара. Содержит общий функционал — отображение названия, категории, цены и изображения.

**Конструктор:**  
`constructor(container: HTMLElement, events: IEvents)` — принимает DOM-элемент карточки и брокер событий.

**Поля класса:**
- `_title: HTMLElement` — элемент названия товара (`.card__title`)
- `_price: HTMLElement` — элемент цены товара (`.card__price`)
- `_category: HTMLElement` — элемент категории (`.card__category`)
- `_image: HTMLImageElement` — элемент изображения (`.card__image`)

**Методы класса:**
- `set title(value: string): void` — устанавливает название товара.
- `set price(value: number | null): void` — устанавливает цену товара. Если `null` — выводит «Бесценно».
- `set category(value: string): void` — устанавливает категорию и CSS-модификатор из `categoryMap`.
- `set image(value: string): void` — устанавливает изображение товара.

---

## Класс CardCatalog

**Назначение:** карточка товара для отображения в галерее (темплейт `card-catalog`). При клике генерирует событие открытия карточки.

**Конструктор:**  
`constructor(container: HTMLElement, events: IEvents)` — принимает DOM-элемент из темплейта `card-catalog` и брокер событий. Вешает обработчик клика на карточку.

Наследует все поля и методы класса `Card`.

---

## Класс CardPreview

**Назначение:** карточка товара для отображения в модальном окне (темплейт `card-preview`). Содержит описание товара и кнопку добавления/удаления из корзины.

**Конструктор:**  
`constructor(container: HTMLElement, events: IEvents)` — принимает DOM-элемент из темплейта `card-preview` и брокер событий.

**Поля класса (дополнительно к Card):**
- `_description: HTMLElement` — элемент описания товара (`.card__text`)
- `_button: HTMLButtonElement` — кнопка «В корзину» / «Удалить из корзины» (`.card__button`)

**Методы класса (дополнительно к Card):**
- `set description(value: string): void` — устанавливает описание товара.
- `set inCart(value: boolean): void` — принимает `boolean`. Меняет текст кнопки: «В корзину» или «Удалить из корзины». Если цена `null` — блокирует кнопку с текстом «Недоступно».

---

## Класс CardBasket

**Назначение:** карточка товара внутри корзины (темплейт `card-basket`). Отображает порядковый номер, название, цену и кнопку удаления.

**Конструктор:**  
`constructor(container: HTMLElement, events: IEvents)` — принимает DOM-элемент из темплейта `card-basket` и брокер событий. Вешает обработчик клика на кнопку удаления.

**Поля класса (дополнительно к Card):**
- `_index: HTMLElement` — элемент порядкового номера (`.basket__item-index`)
- `_deleteButton: HTMLButtonElement` — кнопка удаления товара из корзины (`.basket__item-delete`)

**Методы класса (дополнительно к Card):**
- `set index(value: number): void` — принимает `number`, устанавливает порядковый номер товара в списке.

---

## Класс Basket

**Назначение:** компонент корзины (темплейт `basket`). Отображает список товаров, итоговую стоимость и кнопку оформления заказа.

**Конструктор:**  
`constructor(container: HTMLElement, events: IEvents)` — принимает DOM-элемент из темплейта `basket` и брокер событий. Вешает обработчик клика на кнопку оформления.

**Поля класса:**
- `_list: HTMLElement` — контейнер списка товаров (`.basket__list`)
- `_price: HTMLElement` — элемент итоговой стоимости (`.basket__price`)
- `_button: HTMLButtonElement` — кнопка «Оформить» (`.basket__button`)

**Методы класса:**
- `set items(items: HTMLElement[]): void` — принимает массив DOM-элементов карточек корзины и отображает их в списке. Если массив пустой — блокирует кнопку оформления и выводит сообщение «Корзина пуста».
- `set total(value: number): void` — принимает `number`, устанавливает итоговую стоимость.

---

## Класс Form

**Назначение:** базовый класс для всех форм. Содержит общий функционал — отображение ошибок и управление состоянием кнопки отправки.

**Конструктор:**  
`constructor(container: HTMLFormElement, events: IEvents)` — принимает DOM-элемент формы и брокер событий. Вешает обработчики на события `input` и `submit` формы.

**Поля класса:**
- `_submit: HTMLButtonElement` — кнопка отправки формы
- `_errors: HTMLElement` — элемент для вывода ошибок (`.form__errors`)

**Методы класса:**
- `set valid(value: boolean): void` — принимает `boolean`, управляет активностью кнопки отправки.
- `set errors(value: string): void` — принимает строку с текстом ошибки и выводит её в `.form__errors`.
- `render(data: Partial<T> & IFormState): HTMLElement` — переопределяет метод родителя, принимает данные формы и её состояние.

---

## Класс OrderForm

**Назначение:** форма первого шага оформления заказа (темплейт `order`) — выбор способа оплаты и ввод адреса доставки.

**Конструктор:**  
`constructor(container: HTMLFormElement, events: IEvents)` — принимает DOM-элемент формы `order` и брокер событий. Вешает обработчики на кнопки выбора способа оплаты.

**Поля класса (дополнительно к Form):**
- `_cardButton: HTMLButtonElement` — кнопка «Онлайн» (`button[name="card"]`)
- `_cashButton: HTMLButtonElement` — кнопка «При получении» (`button[name="cash"]`)

**Методы класса (дополнительно к Form):**
- `set payment(value: TPayment | null): void` — принимает `TPayment | null`, выделяет активную кнопку способа оплаты.
- `set address(value: string): void` — принимает строку, устанавливает значение поля адреса.

---

## Класс ContactsForm

**Назначение:** форма второго шага оформления заказа (темплейт `contacts`) — ввод email и телефона.

**Конструктор:**  
`constructor(container: HTMLFormElement, events: IEvents)` — принимает DOM-элемент формы `contacts` и брокер событий.

**Методы класса (дополнительно к Form):**
- `set email(value: string): void` — принимает строку, устанавливает значение поля email.
- `set phone(value: string): void` — принимает строку, устанавливает значение поля телефона.

---

## Класс Modal

**Назначение:** управляет модальным окном (`#modal-container`). Отображает переданный контент, открывает и закрывает окно. Не имеет дочерних классов — весь контент передаётся снаружи.

**Конструктор:**  
`constructor(container: HTMLElement, events: IEvents)` — принимает DOM-элемент модального окна и брокер событий. Вешает обработчики на кнопку закрытия и клик по оверлею.

**Поля класса:**
- `_content: HTMLElement` — контейнер контента модального окна (`.modal__content`)
- `_closeButton: HTMLButtonElement` — кнопка закрытия (`.modal__close`)

**Методы класса:**
- `set content(value: HTMLElement): void` — принимает DOM-элемент и помещает его в `.modal__content`.
- `open(): void` — открывает модальное окно, добавляя класс `modal_active`.
- `close(): void` — закрывает модальное окно, убирая класс `modal_active` и очищая контент.

---

## Класс Success

**Назначение:** экран успешного оформления заказа (темплейт `success`). Отображает сумму списанных синапсов и кнопку возврата к покупкам.

**Конструктор:**  
`constructor(container: HTMLElement, events: IEvents)` — принимает DOM-элемент из темплейта `success` и брокер событий. Вешает обработчик на кнопку закрытия.

**Поля класса:**
- `_description: HTMLElement` — элемент с суммой заказа (`.order-success__description`)
- `_closeButton: HTMLButtonElement` — кнопка «За новыми покупками!» (`.order-success__close`)

**Методы класса:**
- `set total(value: number): void` — принимает `number`, выводит сумму списанных синапсов.
---

# События приложения

В данном разделе описаны все события, которые генерируются в приложении. Все события обрабатываются Презентером.

| Событие | Где генерируется | Описание |
|---|---|---|
| `basket:open` | `Page` | Пользователь нажал на иконку корзины в шапке |
| `card:select` | `CardCatalog` | Пользователь кликнул на карточку товара в каталоге |
| `card:toggleCart` | `CardPreview` | Пользователь нажал кнопку «В корзину» или «Удалить из корзины» в модальном окне товара |
| `cart:remove` | `CardBasket` | Пользователь нажал кнопку удаления товара в корзине |
| `order:start` | `Basket` | Пользователь нажал кнопку «Оформить» в корзине |
| `form:change` | `Form`, `OrderForm` | Пользователь изменил значение поля формы или выбрал способ оплаты |
| `order:submit` | `Form` | Пользователь отправил форму первого шага (способ оплаты и адрес) |
| `contacts:submit` | `Form` | Пользователь отправил форму второго шага (email и телефон) |
| `modal:close` | `Modal` | Пользователь закрыл модальное окно (кнопка или клик по оверлею) |
| `success:close` | `Success` | Пользователь нажал кнопку «За новыми покупками!» |
